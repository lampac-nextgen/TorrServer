/**
 * @deprecated Ghost legacy path — use `shared/theme/colors`.
 * Re-exports the greenfield MatriX palette so leftover imports cannot diverge.
 */
export { brand, getThemeColors, mainColors, themeColors } from 'shared/theme/colors'
export type { ThemeScheme } from 'shared/theme/colors'
